name: JMH Benchmarks

on:
  workflow_dispatch:
    inputs:
      include:
        description: 'Optional regex for benchmark classes to include'
        required: false
        default: ''

jobs:
  jmh:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java-version: ['25', '26']
    permissions:
      contents: read
      packages: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - uses: gradle/actions/wrapper-validation@v4

      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java-version }}
          distribution: 'temurin'
          cache: 'gradle'

      - name: Run JMH (all benchmarks)
        if: ${{ github.event.inputs.include == '' }}
        run: ./gradlew jmh --no-daemon

      - name: Run JMH (filtered)
        if: ${{ github.event.inputs.include != '' }}
        run: ./gradlew jmh --no-daemon -Pjmh.include="${{ github.event.inputs.include }}"

      - name: Upload JMH reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jmh-reports-${{ matrix.java-version }}
          path: |
            build/results/jmh/**
          if-no-files-found: warn
